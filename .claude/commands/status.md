# Estado del Proyecto

Reporte completo del estado actual de TechStock.

## Agente: ops

## Instrucciones
- Git: `git status` + `git log --oneline -10`
- Tests: `pytest --tb=short -q` (ejecutar y reportar)
- Conteo: routers, templates, tests (`pytest --collect-only -q`), cambios pendientes
- Issues: leer memory/project_critical_issues.md si existe
- Presentar reporte estructurado en formato tabla

## Argumentos
$ARGUMENTS
